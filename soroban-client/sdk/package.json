{
  "name": "@crowdpass/tokenbound-sdk",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "generate:types": "node ./scripts/generate-contract-types.mjs",
    "generate:types:v2": "node ./scripts/generate-types-v2.mjs",
    "generate:all": "npm run generate:types && npm run generate:types:v2"
  }
}
